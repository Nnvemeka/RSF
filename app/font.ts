import { Inter, Bebas_Neue, Satisfy } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const bebas_Neue = Bebas_Neue({
  subsets: ["latin"],
  weight: "400",
});

export const satisfy = Satisfy({
  subsets: ["latin"],
  weight: "400",
});
