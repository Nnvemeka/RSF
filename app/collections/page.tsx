import Page from "@/components/page";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "RSF - Collections",
  description: "Created by Edward",
};

const Collections = () => {
  return (
    <Page>
      <h1>Collections page</h1>
    </Page>
  );
};

export default Collections;
